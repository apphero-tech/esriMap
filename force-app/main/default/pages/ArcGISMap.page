<apex:page showHeader="false" sidebar="false" standardStylesheets="false">
    <html>
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="initial-scale=1, maximum-scale=1, user-scalable=no" />
        <title>ArcGIS Map</title>
        <style>
            html, body, #mapView {
                height: 100%;
                width: 100%;
                margin: 0;
                padding: 0;
            }
            #mapView {
                position: relative;
            }
        </style>
        
        <!-- ArcGIS Maps SDK 4.25 -->
        <link rel="stylesheet" href="https://js.arcgis.com/4.25/esri/themes/light/main.css" />
        <script src="https://js.arcgis.com/4.25/"></script>
        
        <script>
            console.log('🚀 Script ArcGIS chargé - début');
            console.log('🔗 Test de connexion vers js.arcgis.com...');
            console.log('📅 Timestamp:', new Date().toISOString());
        </script>
        
        <script>
            console.log('📦 Modules AMD chargés - début');
            
            // Chargement des modules ArcGIS via AMD
            require([
                "esri/Map",
                "esri/views/MapView",
                "esri/geometry/Point",
                "esri/Graphic",
                "esri/symbols/SimpleMarkerSymbol",
                "esri/PopupTemplate",
                "esri/widgets/Popup",
                "esri/widgets/Search",
                "esri/widgets/Compass",
                "esri/widgets/Locate",
                "esri/widgets/BasemapToggle",
                "esri/widgets/Sketch",
                "esri/layers/GraphicsLayer",
                "esri/geometry/geometryEngine",
                "esri/geometry/support/webMercatorUtils",
                "esri/geometry/Extent",
                "esri/core/reactiveUtils"
            ], function(
                Map, MapView, Point, Graphic, SimpleMarkerSymbol, PopupTemplate,
                Popup, Search, Compass, Locate, BasemapToggle, Sketch,
                GraphicsLayer, geometryEngine, webMercatorUtils, Extent, reactiveUtils
            ) {
                console.log('✅ Tous les modules ArcGIS 4.25 chargés avec succès via AMD');
                
                // Variables globales
                let map, view, sketchWidget, sketchLayer;
                let isReadOnlyMode = false;
                let currentDrawingGraphic = null;

                // Initialisation de la carte
                async function initializeMap() {
                    try {
                        console.log('🗺️ Initialisation de la carte ArcGIS...');
                        
                        // Créer la carte
                        map = new Map({
                            basemap: "streets-vector"
                        });

                        // Créer la vue
                        view = new MapView({
                            container: "mapView",
                            map: map,
                            center: [-0.1276, 51.5074], // Londres par défaut
                            zoom: 10,
                            // DÉSACTIVER le zoom clavier pour éviter le conflit avec Chrome
                            constraints: {
                                keyboardNavigation: {
                                    enabled: false
                                }
                            },
                            // DÉSACTIVER les raccourcis clavier ArcGIS
                            popup: {
                                dockEnabled: false
                            }
                        });

                        // Attendre que la vue soit prête
                        await view.when();
                        console.log('✅ Vue de carte initialisée');

                        // Créer la couche de dessin
                        sketchLayer = new GraphicsLayer();
                        map.add(sketchLayer);

                        // Initialiser les widgets
                        await initializeWidgets();
                        
                        // Charger la configuration des outils
                        await loadDrawingToolsConfig();
                        
                        // Configurer la communication postMessage
                        setupPostMessageCommunication();
                        
                        // Notifier que la carte est prête
                        notifyMapReady();
                        
                    } catch (error) {
                        console.error('❌ Erreur initialisation carte:', error);
                    }
                }

                // Initialiser les widgets
                async function initializeWidgets() {
                    try {
                        // Widget de recherche
                        const searchWidget = new Search({
                            view: view,
                            includeDefaultSources: false,
                            sources: [{
                                url: "https://geocode.arcgis.com/arcgis/rest/services/World/GeocodeServer",
                                singleLineFieldName: "SingleLine",
                                name: "ArcGIS World Geocoding Service",
                                placeholder: "Rechercher une adresse...",
                                maxResults: 3,
                                maxSuggestions: 6,
                                minSuggestCharacters: 3
                            }]
                        });
                        view.ui.add(searchWidget, "top-left");

                        // Widget boussole
                        const compassWidget = new Compass({
                            view: view
                        });
                        view.ui.add(compassWidget, "top-left");

                        // Widget de localisation
                        const locateWidget = new Locate({
                            view: view,
                            useHeadingEnabled: false
                        });
                        view.ui.add(locateWidget, "top-left");

                        // Widget de basculement de fond de carte
                        const basemapToggle = new BasemapToggle({
                            view: view,
                            nextBasemap: "satellite"
                        });
                        view.ui.add(basemapToggle, "bottom-right");

                        console.log('✅ Widgets initialisés');
                        
                    } catch (error) {
                        console.error('❌ Erreur initialisation widgets:', error);
                    }
                }

                // Charger la configuration des outils de dessin
                async function loadDrawingToolsConfig() {
                    try {
                        console.log('🔧 Chargement configuration outils de dessin...');
                        
                        // Créer et activer le widget Sketch
                        sketchWidget = new Sketch({
                            view: view,
                            layer: sketchLayer,
                            creationMode: "hybrid"
                        });
                        
                        view.ui.add(sketchWidget, "top-right");
                        console.log('✅ Widget Sketch initialisé');
                        
                        // Écouter les événements de dessin
                        sketchWidget.on("create", function(event) {
                            if (event.state === "complete") {
                                console.log('✅ Forme dessinée:', event.graphic);
                                currentDrawingGraphic = event.graphic;
                                notifyDrawingComplete(event.graphic);
                            }
                        });
                        
                        sketchWidget.on("update", function(event) {
                            console.log('🔄 Forme mise à jour:', event.graphics);
                        });
                        
                        sketchWidget.on("delete", function(event) {
                            console.log('🗑️ Forme supprimée');
                            currentDrawingGraphic = null;
                            notifyDrawingDeleted();
                        });
                        
                    } catch (error) {
                        console.error('❌ Erreur configuration outils dessin:', error);
                    }
                }

                // Configurer la communication postMessage
                function setupPostMessageCommunication() {
                    try {
                        console.log('📡 Configuration communication postMessage...');
                        
                        // PROTECTION ANTI-CRASH : Intercepter les touches de zoom Chrome
                        document.addEventListener('keydown', function(event) {
                            // Intercepter Cmd+ et Cmd- (zoom Chrome)
                            if (event.metaKey && (event.key === '+' || event.key === '-' || event.key === '=')) {
                                console.log('🛡️ Protection anti-crash : Zoom clavier intercepté');
                                event.preventDefault();
                                event.stopPropagation();
                                return false;
                            }
                        }, true);
                        
                        window.addEventListener('message', function(event) {
                            console.log('📨 Message reçu:', event.data);
                            
                            if (event.data.type === 'GET_MAP_STATE') {
                                sendMapState();
                            } else if (event.data.type === 'CLEAR_MAP') {
                                clearMap();
                            } else if (event.data.type === 'SET_READONLY_MODE') {
                                setReadOnlyMode(event.data.readOnly);
                            }
                        });
                        
                        console.log('✅ Communication postMessage configurée');
                        
                    } catch (error) {
                        console.error('❌ Erreur configuration postMessage:', error);
                    }
                }

                // Effacer la carte
                function clearMap() {
                    try {
                        sketchLayer.removeAll();
                        currentDrawingGraphic = null;
                        console.log('🗑️ Carte effacée');
                        
                        // Notifier la suppression
                        notifyDrawingDeleted();
                        
                    } catch (error) {
                        console.error('❌ Erreur effacement carte:', error);
                    }
                }

                // Définir le mode lecture seule
                function setReadOnlyMode(readOnly) {
                    try {
                        isReadOnlyMode = readOnly;
                        
                        if (readOnly) {
                            sketchWidget.disable();
                            console.log('🔒 Mode lecture seule activé');
                        } else {
                            sketchWidget.enable();
                            console.log('✏️ Mode édition activé');
                        }
                        
                    } catch (error) {
                        console.error('❌ Erreur changement mode:', error);
                    }
                }

                // Envoyer l'état de la carte
                function sendMapState() {
                    try {
                        const mapState = {
                            type: 'MAP_STATE',
                            hasGeometry: sketchLayer.graphics.length > 0,
                            geometryCount: sketchLayer.graphics.length,
                            center: view.center,
                            zoom: view.zoom
                        };
                        
                        window.parent.postMessage(mapState, '*');
                        console.log('📤 État de la carte envoyé:', mapState);
                        
                    } catch (error) {
                        console.error('❌ Erreur envoi état carte:', error);
                    }
                }

                // Notifier que la carte est prête
                function notifyMapReady() {
                    try {
                        const message = {
                            type: 'MAP_READY',
                            timestamp: new Date().toISOString()
                        };
                        
                        window.parent.postMessage(message, '*');
                        console.log('📤 Carte prête notifiée');
                        
                    } catch (error) {
                        console.error('❌ Erreur notification carte prête:', error);
                    }
                }

                // Notifier qu'un dessin est complet
                function notifyDrawingComplete(graphic) {
                    try {
                        const message = {
                            type: 'DRAWING_COMPLETE',
                            geometry: graphic.geometry,
                            timestamp: new Date().toISOString()
                        };
                        
                        window.parent.postMessage(message, '*');
                        console.log('📤 Dessin complet notifié');
                        
                    } catch (error) {
                        console.error('❌ Erreur notification dessin complet:', error);
                    }
                }

                // Notifier qu'un dessin a été supprimé
                function notifyDrawingDeleted() {
                    try {
                        const message = {
                            type: 'DRAWING_DELETED',
                            timestamp: new Date().toISOString()
                        };
                        
                        window.parent.postMessage(message, '*');
                        console.log('📤 Suppression dessin notifiée');
                        
                    } catch (error) {
                        console.error('❌ Erreur notification suppression:', error);
                    }
                }

                // Initialiser la carte quand le DOM est prêt
                document.addEventListener('DOMContentLoaded', function() {
                    console.log('🚀 DOM chargé, initialisation de la carte...');
                    initializeMap();
                });
                
                // FORCER l'initialisation immédiate (au cas où DOMContentLoaded ne se déclenche pas)
                console.log('🚀 Tentative d\'initialisation immédiate...');
                setTimeout(function() {
                    if (!map) {
                        console.log('⚠️ DOMContentLoaded non déclenché, initialisation forcée...');
                        initializeMap();
                    }
                }, 1000);
                
            }); // Fin du require AMD
        </script>
    </head>
    <body>
        <div id="mapView"></div>
    </body>
    </html>
</apex:page>
